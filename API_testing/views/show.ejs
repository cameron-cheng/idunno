<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RESULTS</title>
</head>
<body>
  <div style="display: flex; flex-direction: column; align-items: center">
    <% results.forEach((result => { %>
      <h1><%= result.name %></h1>
      <h2><%= result.rating %> / 5</h2>
      <h3><%= result.formatted_address %></h3>
      <img src='https://maps.googleapis.com/maps/api/place/photo?key=<%= API_KEY %>&photoreference=<%= result.photos[0].photo_reference %>&maxheight=400'>
      <a href='/details/<%= result.place_id %>'>RESTAURANT DETAILS</a>
      <p>=================================================================</p><br>
    <% })) %>
  </div>
</body>
</html>